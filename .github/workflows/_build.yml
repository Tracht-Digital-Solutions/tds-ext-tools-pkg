name: Build (reusable)

# Single source of truth for this extension. Called by ci.yml (PR gate, no
# publish), dev.yml (prerelease -> @dev) and release.yml (version bump ->
# @latest + push tag). Dual package: the TS half publishes to GitHub Packages;
# the PHP half is a Composer package consumed via VCS, so its release IS the git
# tag pushed here. Both test suites gate every run.
#
# NB NPM_TOKEN is set from PACKAGE_TOKEN (needs read:packages) so `npm install`
# can pull @tracht-digital-solutions/tds-frontend-contract from GitHub Packages via
# the committed .npmrc; the same token (write+delete:packages) publishes.

on:
  workflow_call:
    inputs:
      publish_tag:
        description: "none = build only (CI); 'dev' = prerelease @dev; 'latest' = real release."
        required: false
        type: string
        default: none
      bump:
        description: Version bump for a 'latest' release (patch | minor | major).
        required: false
        type: string
        default: patch
      rebuild_site:
        description: Repo whose workflow to dispatch after a @latest publish (site rebuild). Empty = none.
        required: false
        type: string
        default: ''
      rebuild_workflow:
        description: >-
          Which workflow to dispatch in rebuild_site. Defaults to dev.yml, which
          builds the staging artifact WITHOUT deploying. Do not point this at a
          product's release.yml: since 2026-08-25 the frontend products are Node
          applications, so their release branch is a running app rather than a
          folder of files, and a push to it deploys. A commit in THIS repo must
          not be able to deploy someone else's panel to production.
        required: false
        type: string
        default: dev.yml

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      NPM_TOKEN: ${{ secrets.PACKAGE_TOKEN }}
    steps:
      - uses: actions/checkout@v5
        with:
          token: ${{ secrets.PACKAGE_TOKEN }}

      # --- PHP half: the Module + its route ----------------------------------
      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
          tools: composer:v2

      - name: Composer validate
        run: composer validate --no-check-publish --no-check-lock

      - name: Composer install
        # Resolves tds-frontend-contract from its public VCS repo (the local path
        # repo in composer.json is a dev convenience and is absent in CI).
        uses: ramsey/composer-install@v3

      - name: PHP syntax check
        run: |
          find php/src php/tests -name '*.php' -print0 \
            | xargs -0 -n1 -P4 php -l > /dev/null

      - name: PHPUnit
        run: composer test

      # --- TS half: the manifest ---------------------------------------------
      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Install
        # `npm install --no-package-lock` (not `npm ci`): a Windows-generated
        # lockfile pins only win32 optional binaries and breaks the Linux
        # runner (npm bug #4828). Auth via the committed .npmrc + NPM_TOKEN.
        run: npm install --no-package-lock

      - name: Type check
        run: npm run type-check

      - name: Lint primitives
        # Extensions ship no CSS of their own, so a control without the
        # shared class is unstyled — and an unclassed <input> is INVISIBLE,
        # because Tailwind preflight zeroes borders. 86 buttons and 105
        # controls had drifted across the packages before this step existed.
        run: npm run lint:primitives

      - name: Test (vitest)
        # phpunit runs above; the island suite did not run at all, so ~1 200
        # lines of admin-UI tests gated nothing.
        run: npm run test:run

      - name: Build
        run: npm run build

      # --- Publish -----------------------------------------------------------
      - name: Publish prerelease (@dev)
        if: ${{ inputs.publish_tag == 'dev' }}
        run: |
          base=$(node -p "require('./package.json').version")
          npm version "${base}-dev.${{ github.run_number }}" --no-git-tag-version
          npm publish --tag dev

      - name: Release (@latest) + push tag
        # Bumps package.json AND composer.json in lockstep (one release),
        # commits, tags, publishes npm, pushes the tag (= the Composer release ref).
        if: ${{ inputs.publish_tag == 'latest' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          new=$(npm version ${{ inputs.bump }} --no-git-tag-version)
          # composer.json AND the extension manifest, from the version npm just
          # wrote. The manifest is what the panel's Module page reports, and
          # nothing used to bump it — it sat at 0.1.0 while the package reached
          # 0.1.22. packaging.test.ts asserts all three agree.
          node scripts/sync-version.mjs
          git add package.json composer.json src/index.ts
          git commit -m "release: ${new} [skip ci]"
          # Annotated tag (-a): `git push --follow-tags` pushes ONLY annotated
          # tags — a lightweight tag never reaches the remote, so Composer (which
          # resolves this package by VCS tag) would never see the release.
          git tag -a "${new}" -m "release ${new}"
          npm publish
          git push --follow-tags origin HEAD:main

      - name: Dispatch site rebuild
        # After a real release, kick the dependent product so it rebuilds
        # against the new extension version. This targets rebuild_workflow
        # (dev.yml by default) — a BUILD, not a deploy. It used to name
        # release.yml, which meant every push to this repo's main deployed
        # tds-admin-frontend to production; harmless while that was a folder of
        # static files, and a panel-wide outage once it became a Node app.
        if: ${{ inputs.publish_tag == 'latest' && inputs.rebuild_site != '' }}
        env:
          GH_TOKEN: ${{ secrets.PACKAGE_TOKEN }}
        run: gh workflow run "${{ inputs.rebuild_workflow }}" -R Tracht-Digital-Solutions/${{ inputs.rebuild_site }}

      - name: Prune old prerelease package versions (keep newest 5)
        continue-on-error: true  # best-effort housekeeping; needs delete:packages
        if: ${{ inputs.publish_tag != 'none' }}
        uses: actions/delete-package-versions@v5
        with:
          package-name: tds-ext-tools
          package-type: npm
          min-versions-to-keep: 5
          delete-only-pre-release-versions: 'true'
          token: ${{ secrets.PACKAGE_TOKEN }}

      - name: Prune old stable package versions (keep newest 10)
        continue-on-error: true  # best-effort housekeeping; needs delete:packages
        if: ${{ inputs.publish_tag == 'latest' }}
        uses: actions/delete-package-versions@v5
        with:
          package-name: tds-ext-tools
          package-type: npm
          min-versions-to-keep: 10
          ignore-versions: '^.*-.*$'
          token: ${{ secrets.PACKAGE_TOKEN }}
