---
// Tools settings section — AdSense + rebuild + registry config, persisted to the
// core settings store (/admin/settings/tools).
import ToolsSettings from "./ToolsSettings.tsx";
---

<div class="tds-settings-section__body">
  <h3>Tools / AdSense</h3>
  <ToolsSettings client:load />
</div>
